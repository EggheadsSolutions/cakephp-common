
version: '3'

services:
    php:
        build: ./dev/php
        image: eggheads-php8
        volumes:
            - .:/var/www/
        environment:
            XDEBUG_CONFIG: remote_host=host.docker.internal
